#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{

    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    long long int a, b;
    scanf("%lld %lld", &a, &b);

    long long int multiply = a * b;
    printf("%lld", multiply);
    return 0;
}
