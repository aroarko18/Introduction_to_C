#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    // double x;
    // scanf("%lf", &x);
    // // int ans = ceil(x);
    // // int ans = floor(x);
    // float ans = round(x);
    // // printf("%d\n", ans);
    // printf("%0.2f", ans);

    // double x;
    // scanf("%lf", &x);

    // double ans = sqrt(x);
    // printf("%0.2lf\n", ans);

    // int a, b;
    // scanf("%d %d", &a, &b);

    // int ans = pow(a, b);
    // printf("%d\n", ans);

    int x;
    scanf("%d", &x);
    int ans = abs(x);
    printf("%d", ans);
    return 0;
}